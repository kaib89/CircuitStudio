[project]
name = "circuitstudio"
version = "0.1.0"
description = "Schematic editor: the LLM writes the netlist, you arrange the drawing."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
license = "Apache-2.0"
license-files = ["LICENSE"]

[tool.uv]
package = false
